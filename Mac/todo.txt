### Change
x all `wget` references to `curl -o`
x all `brew cask install` to `brew install`
x separate sections like all brew things, curl things, etc.
x remove references to ip addresses

### Add
- iterm2 installation
- oh-my-zsh installation
- iterm2 configuration
